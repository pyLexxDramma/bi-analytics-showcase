# Приёмка: Showcase Next (`webapp`) ↔ `[main]` Streamlit

> **Полный повторный проход (все экраны заново):** см. отдельный план  
> [`PARITY_FULL_REGRESSION_PLAN.md`](./PARITY_FULL_REGRESSION_PLAN.md).  
> Этот файл остаётся журналом деталей по §; статусы нового цикла — в плане регресса.  
> **Mobile-фильтры (все дашборды, 03.08.2026):** на `<lg` — bottom sheet + крупные чипы (≥44px). **Desktop — как main:** select/date/text, не chips (кроме экранов, где chips есть в Streamlit). См. `PARITY_FULL_REGRESSION_PLAN.md`.
>
> Цикл 02.08–04.08.2026: **§0–§14 ✅** → **§15** Исполнительная документация ⬜.
>
> **Mobile-таблицы (все дашборды):** на `<lg` таблицы → блоки/карточки (`MobileEntityCard` / эквивалент) с сохранением подсветки (красный/зелёный), выделений и шрифтов desktop. Аудит §1–§9: см. `PARITY_FULL_REGRESSION_PLAN.md` §«Аудит mobile-таблиц».
>
> **Desktop-графики Plotly (все дашборды, правило 03.08.2026):** на `lg+` — **1:1 с main**: Plotly, подписи значений, точки/маркеры, цвета серий, легенда/оси/modebar (все вкладки экрана, напр. §10 main+delay). **Только mobile** — где уместно облегчать (высота, modebar, плотность подписей); цифры/серии те же.

Цикл на экран (как договорились):

1. **Отображение** — блоки/KPI/графики/таблицы vs main  
2. **Фильтры / чекбоксы / radio** — полный набор, дефолты, зависимости  
3. **Цифры** — те же срезы → те же KPI/ячейки  
4. Скрины от пользователя → агент сверяет  
5. **OK** → commit/push showcase  

Техфундамент (порядок работ):

1. ✅ Ingest → `web_data.db` (`POST /api/admin/ingest`, sync = FTP+ingest)  
2. ✅ Disk-кэш payload’ов + фоновые jobs (анти-502), состояние jobs на диске — работает при нескольких воркерах  
3. ✅ `services/core_bridge.py` — единая точка входа в код `[main]` (stub, path, БД, `dashboards.*`)  
4. ✅ Shared chrome: `DownloadTableButton` (CSV `;`+BOM / xlsx) и `FullscreenPanel` (зум, Esc)  
5. ⏳ API тонкий слой поверх БД/кэша, фильтры 1:1  
6. ⏳ Убрать упрощения в services (`finance_period` → `finance_1c` из main)  
7. ⏳ Матрица ниже → ✅ по экранам  

Источник порядка: `REPORT_CATEGORIES` в `[main]` / `nav.ts` в webapp.

Проверки перед приёмкой экрана (все запускаются из `webapp/`):

| Скрипт | Что проверяет |
|--------|----------------|
| `python scripts/parity_smoke.py <экран>` | KPI API против эталона, посчитанного функциями main по активной версии БД |
| `python scripts/check_core_bridge.py` | stub/импорт `dashboards.*`/чтение БД не сломаны |
| `python scripts/check_jobs_store.py` | job виден из другого процесса (несколько воркеров) |
| `python scripts/check_data_sources.py` | сервис не начал читать `web/` вместо `web_data.db` |

Доступ для скринов: UI за логином (`/login`), на VPS вход под demo-ролью; API-проверки идут напрямую на `:8000`/`:3080` без авторизации.

| # | Экран | Маршрут webapp | Статус | Скачать | Зум | Заметки |
|---|--------|----------------|--------|---------|-----|---------|
| 1 | Девелоперские проекты | `/developer-projects` | ✅ принят на стенде 30.07.2026 | ✓ CSV+xlsx (shared) | ✓ (shared) | API=матрица main; smoke зелёный (version_id, ячейки заполнены) |
| 2 | БДДС (расходы) | `/finance/bdds` | ✅ принят на стенде 30.07.2026 | ✓ CSV+xlsx (shared, 2 таблицы) | ✓ (shared, график тянется на весь экран) | smoke ALL OK на version_id=2: main 11992.3/5824.0 = api, 32/32 месяца, `mode=synthetic_1c` |
| 3 | БДР (расходы) | `/finance/bdr` | ✅ принят на стенде 02.08.2026 (цифры) | ✓ CSV+xlsx (shared, 2 таблицы) | ✓ (shared) | ИТОГО 8169.9/5881.0/−2288.9; фильтр периода = main (`5b84fec`) |
| 4 | Утверждённый бюджет план/факт | `/finance/approved-budget` | ✅ принят на стенде 02.08.2026 (регресс) | ✓ CSV+xlsx (2 таблицы) | ✓ (график и таблицы) | ИТОГО 13207/5881/−7326; проекты/срезы = main; SVG-gauge + Tremor |
| 5 | БДДС расходы (план, факт, уточн.) | `/finance/bdds-plan-fact` | ✅ принят на стенде 02.08.2026 | ✓ CSV+xlsx (2 таблицы) | ✓ (график+таблицы) | ИТОГО 6716.1/3889.8/6716.1; редактор лотов; mobile fit (`533d0c8`) |
| 6 | Контрольные точки | `/timeline/control-points` | ✅ принят на стенде 03.08.2026 (регресс) | ✓ CSV+xlsx | ✓ | цифры=main; mobile cards+pills; desktop fit (`a074d6a`) |
| 7 | График проекта | `/timeline/project-schedule` | ✅ принят на стенде 03.08.2026 (регресс) | ✓ CSV+xlsx | ✓ | ковенанты=main; date pad; mobile compact gantt+cards (`51b4758`) |
| 8 | Причины отклонений | `/timeline/deviation-reasons` | ✅ принят на стенде 03.08.2026 (регресс) | ✓ CSV+xlsx | ✓ | Plotly 1:1; y=0; modebar desktop; mobile cards+без modebar; download (`182242d`) |
| 9 | Отклонение от базового плана | `/timeline/baseline-deviation` | ✅ принят на стенде 03.08.2026 (регресс) | ✓ CSV+xlsx | ✓ | ЗОС=main; ковенанты points+таблица; фильтры kit; mobile cards; ⛶ под modebar (`de0fa01`) |
| 10 | Проектная документация | `/docs/project-documentation` | ✅ принято на стенде 03.08.2026 (регресс) | ✓ CSV+xlsx (3 таблицы) | ✓ | Plotly pie/line/monthly/gantt 1:1; mobile cards; правило графиков |
| 11 | Рабочая документация | `/docs/working-documentation` | ✅ принято на стенде 03.08.2026 (регресс) | ✓ CSV+xlsx | ✓ | Plotly 1:1; overdue~520; chips BDDS+scroll; mobile cards (`c9e115f`) |
| 12 | ГДРС (люди) | `/gdrs/people` | ✅ принято на стенде 03.08.2026 | ✓ CSV+xlsx | ✓ | Plotly desktop+mobile; pie legend; dynamics scroll; chips; Итого |
| 13 | ГДРС (техника) | `/gdrs/equipment` | ✅ принято на стенде 03.08.2026 (регресс) | ✓ CSV+xlsx | ✓ | Тот же UI что §12 (`GdrsView`); vid=Техника |
| 14 | Предписания по подрядчикам | `/prescriptions` | ✅ принято на стенде 31.07.2026 | ✓ CSV+xlsx | ✓ | БД через core_bridge; см. §14 |
| 15 | Исполнительная документация | `/executive-docs` | ✅ принято на стенде 31.07.2026 | ✓ CSV+xlsx | ✓ | БД через core_bridge; см. §15 |
| 16 | ДЗ/КЗ подрядчиков | `/debit-credit` | ✅ принято на стенде 31.07.2026 | ✓ CSV+xlsx | ✓ | БД через core_bridge; см. §16 |
| 17 | Настройки профиля | `/settings/profile` | ✅ принято на стенде 31.07.2026 | — | — | users.db; admin/суперадмин; см. §17 |
| 18 | Административная панель | `/settings/admin` | ✅ принято на стенде 31.07.2026 | ✓ логи | — | фильтры + система + FTP; см. §18 |

Легенда: ⬜ не начат · 🔄 в работе · 🐛 баги по скринам · ✅ OK (после commit).  
Колонки «Скачать»/«Зум»: ✓ — есть и подтверждено кликом, ✗ — нет (или есть в main, но не перенесено).

---

## §16 Дебиторская и кредиторская задолженность подрядчиков

### Локально готово 31.07.2026

Статус: ✅ принято на стенде 31.07.2026.

- API `debit_credit.py`: active `web_data.db` — `debit_credit` + `reference_dannye`; суммы через `_to_num` (запятые тысяч).
- КС-2: точные статьи casefold + match по ID_Договора / ID_Контрагента.
- График: top-20 контрагентов; «Без группировки» (group blue/amber/gray/rose) и «С группировкой» (stack gray/amber/blue).
- Таблица млн ₽ + sticky ИТОГО + 🟢🟡🔴 + CSV/xlsx + FullscreenPanel.
- Локально version 4 ИТОГО млн: договор ~17795 · выполнено ~4966 · аванс ~2436 · КС-2 ~2530 (скрин main ~18877/4945/2415 — другой срез).

### Проверить в UI / на стенде

1. Фильтры: Проект, Подрядчик, № договора, период, Отображение (оба режима), Сбросить.
2. Без группировки: отрицательные красные столбцы; с группировкой: стек без отрицательных.
3. Таблица/ИТОГО/светофор/легенда/скачать/fullscreen; светлая и тёмная тема.
4. KPI близки к main на том же срезе данных.

---

## §1 Девелоперские проекты

### Сверка скринов 2026-07-30 (version_id=1)

**Цифры сходятся.** Построчно совпали все 6 проектов на видимых колонках (Аренда ЗУ,
Готовый Продукт, ГПЗУ, Экспертиза стадии ст П, КОМАНДА РП, РС), включая знаковые случаи:
Дмитровский ГПЗУ +91 дн., Есипово-5 (1 и 2 этап) ГПЗУ −270 дн., Жуковский Аренда ЗУ −46 дн.
и РС −61 дн., Ленинский Экспертиза −60 дн. Совпали порядок строк и колонок, деление
Есипово-5 на этапы, плашка о неполных данных (снимок MSP 28.07.2026), цветовая логика.

### Расхождения обвязки и решения

| Что | `[main]` :8501 | webapp | Решение |
|-----|----------------|--------|---------|
| KPI-карточки + 2 графика | нет, экран = только матрица | были | ✅ убраны (запись «KPI/charts убраны» ранее была неверной — они оставались в коде) |
| Фильтр | expander «Фильтры» + «Сбросить» + multiselect | чипы без «Сбросить» | ✅ добавлены сворачиваемый блок «Фильтры» и «Сбросить» |
| Легенда | одна строка под таблицей | карточка в 3 строки | ✅ сжата в одну строку, тексты main (100% План/Факт, +0, −0) |
| Скачать / зум | кнопка под таблицей слева, зум в углу | обе кнопки над матрицей справа | ✅ «Скачать таблицу» под таблицей слева, в углу только ⛶ |
| Служебная строка «Режим данных: ftp · version_id…» | нет | есть | ✅ убрана (индикатор загрузки перенесён в тело таблицы) |
| Подзаголовок под H1 | нет | «Контрольные точки проектов…» | ✅ убран |
| Блок «Фильтры» | свёрнут по умолчанию | развёрнут | ✅ свёрнут по умолчанию |
| Выбор версии данных в сайдбаре | есть (`created_at \| файлов, строк` + ✅) | нет | ✅ добавлен: `GET /api/versions`, переключение через `POST /api/admin/versions/{id}/activate` (нужен admin-токен, как для FTP) |

### Сетка таблицы

Границы перенесены из `_DEV_TZ_MATRIX_CSS_LIGHT` / `_DEV_TZ_MATRIX_CSS_DARK` main: рамка 3px `#94a3b8`,
внутренние линии 1px `#cbd5e1`, толстые 3px по границам блоков (колонка «Проект», фазы, каждая
контрольная точка), низ шапки 2px. Заголовки точек и подписи План/Факт/Откл. окрашены в цвет блока
(`#dcfce7` инвестфаза, `#e2e8f0` жизнь проекта), «зебра» строк убрана. Тёмная пара — белые толстые
линии, 1px `#5a6f82`, фон ячеек `#0c1219`, колонка проектов `#161f2b`.

### Общая обвязка (влияет на все экраны)

- Фуллскрин (`FullscreenPanel`): таблица центрируется, обёртка `min-w-fit` (иначе срезается левый край),
  кнопка выхода `fixed` в углу.
- Сайдбар: `sticky` на всю высоту, прокрутка меню включается только под курсором.

### Приёмка

Принят пользователем 30.07.2026 — сначала локально (цифры, сетка, легенда, фильтры, скачивание, зум),
затем на стенде после двух деплой-фиксов:

1. `No module named 'pytz'` — в образе API не было зависимости кода main (`utils.py`, `logger.py`);
   добавлен `pytz>=2024.1` в `webapp/api/requirements.txt`.
2. `cannot import name '_build_project_frames' from 'web_loader'` — `/core` монтируется из копии ядра
   в этом репозитории, и она отстала от main; выборочно синхронизированы `web_loader.py`,
   `web_schema.py`, новый `web_db_read.py` с повторным наложением showcase-патчей.

Проверка стенда: `meta.error` пуст, 6 проектов / 21 колонка, подписи проектов русские.
Урок: локальные прогоны надо делать с `BI_CORE_APP_DIR`, указывающим на копию ядра, иначе стенд
расходится с локалью; деплой при правках вне `webapp/**` workflow сам не запускает.

✅ экран закрыт — следующий §2 БДДС

---

## §2 БДДС (расходы)

### Источник данных

`services/finance_1c.py` → `load_bdds_screen_frame` повторяет весь путь экрана
`dashboard_budget_by_period`, а не одну функцию: календарь из MSP, фильтр MSP по пересечению
интервалов, сужение синтетики 1С до проектов MSP (`restrict_projects_from_df`), overlay с
дедупом по norm-key, обнуление плана без сценария «ПЛАН», досборка сетки месяцев, finalize.
Вход — `reference_dannye` активной версии `web_data.db`. Чтение `web/` и `finance_period` из
БДДС убраны, `meta.mode=synthetic_1c`, `meta.rows_1c` = размер снимка оборотов, `meta.error`
выводится в UI карточкой ошибки.

### Сверка цифр (version_id=2, снимок 1С 2499 строк, 2026-07-30)

`python scripts/parity_smoke.py bdds` → ALL OK: план 11992.3 млн, факт 5824.0 млн,
отклонение −6168.3 (main = api), период 2024-03-23 — 2029-07-30, 32 месячные строки без
расхождений, `mode=synthetic_1c`. По проектам: Дмитровский-1 2192.9/1441.0,
Есипово-5 4057.9/1888.3, Ленинский 5741.5/2494.7.

Эталон в smoke считается на **БД основного дашборда** (`BI_MAIN_WEB_DB_PATH` / `BI_MAIN_APP_DIR`
/ автоопределение рядом с showcase-репо), БД сервиса берётся из `meta.db`, дополнительно
сверяется размер снимка 1С. Это важно: пока эталон читал ту же базу, что и API, устаревшие
данные сервиса давали ложный зелёный. Ранние 12058.6/5762.0 — не ошибка расчёта, а снимок
оборотов на 29.07 (2470 строк); после FTP-синка и ingest цифры совпали с экраном main.

**Стенд:** данные обновляет сам деплой — `server_deploy.sh` вызывает `run_ftp_then_db_ingest`
внутри контейнера api (там admin-токен не нужен), отключается `WEBAPP_SKIP_SYNC=1`. Проверено
на стенде: версия 2, снимок 2499 строк, ИТОГО 11992.3 / 5824.0, проекты 2192.9/1441.0,
4057.9/1888.3, 5741.5/2494.7 — совпало с main, экран принят пользователем 30.07.2026.

### Фильтры и chrome (по `dashboard_budget_by_period`)

| Элемент main | webapp |
|--------------|--------|
| multiselect проектов, пусто = все | чипы + «Все», блок «Фильтры» свёрнут, есть «Сбросить» |
| диапазон по «Конец план» | два `date` с границами из данных |
| «Группировать по» Месяц/Квартал/Год | select `group=month\|quarter\|year` |
| «Представление» По месяцам/Накопительно | select `view=monthly\|cumulative` |
| «Показать отклонение» (по умолчанию off) | чекбокс, добавляет серию на график |
| «Скрывать месяцы, где план и факт равны 0» (on, когда проект не выбран) | чекбокс, активен только для Месяц+По месяцам, порог 0.5 млн как в main |
| таблица «БДДС …»: Проект / Период / План / Факт / Отклонение + ИТОГО | та же, блоки по проектам при >1 проекте, отклонение красное/зелёное |
| таблица «БДДС по проектам» + ИТОГО | есть |
| график Plotly: подписи значений, легенда снизу, панель инструментов | `FinanceBarChart` на Plotly, те же цвета серий, подписи «N.N млн. руб.», отклонение двумя цветами |
| Скачать / зум | `DownloadTableButton` под каждой таблицей слева, `FullscreenPanel` в углу |

Зум графика: в фуллскрине высота = высота окна −32px, ширина 150px на период (минимум — ширина
окна) с горизонтальной прокруткой, поэтому столбцы не сжимаются и видны целиком; подписи
укрупняются (значения от 14px, тики 14px, заголовки осей 17px, легенда 15px). `FullscreenPanel`
поддерживает режим `fill` и передаёт признак зума детям.

### Осознанные отличия

- Квартал/год — перегруппировка месячной сетки, а не буквальный путь main (там на не-showcase
  режиме overlay падает на смешанных частотах `Period`).
- Колонка «Скорректированный бюджет» не выводится.
- Таблица «БДДС план-фактный анализ» (в main появляется при выборе одного проекта) не переносилась —
  MVP-cut, требует `build_bdds_plan_fact_analysis_table` и оборотов; кандидат в фазу 2.5.

---

## §3 БДР (расходы)

### Статус приёмки 02.08.2026 (цикл регресса)

✅ **Цифры + chrome** приняты на стенде 02.08.2026.

Период сверки: **31.08.2025 – 31.08.2026**, проект «Все»; группы месяц/квартал/год.

| Источник | план, млн | факт, млн | откл., млн |
|----------|----------:|----------:|-----------:|
| Main (скрины) | **8169.9** | **5881.0** | **−2288.9** |
| Showcase стенд после `5b84fec` | **8169.9** | **5881.0** | **−2288.9** |

Проекты (showcase = main): Дмитровский 1589.2/1441.0/−148.2; Есипово-5 2742.4/1921.5/−820.9; Ленинский 3838.3/2518.4/−1319.8.

**Баг и фикс:** фильтр `plan end` по `.dt.date` включал август 2026 (`23:59:59.999999`) → план 9444.7. Выровнено с main/БДДС: `date_to + 1 day − 1 second` (`finance_1c.load_bdr_screen_frame`, cache `bdr` v3, коммит `5b84fec`).

Chrome ранее: `e62e7eb` (30.07.2026). UI = `BddsView` + fetchBdr; chart hint «Подписи на столбцах — млн ₽.» (shared с БДДС).

---

## §4 Утверждённый бюджет план/факт

### Статус приёмки 02.08.2026 (цикл регресса)

✅ Принят на стенде 02.08.2026 (цифры + chrome; mobile в общем чеклисте экрана).

Ранее chrome: `59f6431` (30.07.2026).

### Сверка цифр (одна version, скрины main 1–4 / showcase 5–8)

| Срез | план, млн | факт, млн | откл., млн |
|------|----------:|----------:|-----------:|
| Все | **13207.0** | **5881.0** | **−7326.0** |
| Дмитровский | 2322.9 | 1441.0 | −881.9 |
| Есипово-5 | 4490.1 | 1921.5 | −2568.6 |
| Ленинский | 6393.9 | 2518.4 | −3875.5 |

API `parity=main_bdds_cumulative`, `mode=synthetic_1c`. Таблица проектов: 2322.94/1441.05, 4490.11/1921.50, 6393.92/2518.43.

### Chrome / осознанные отличия

- Фильтры: Проект, ФИЗ, Сбросить, hide_zero / show_deviation; SVG-gauge; Tremor вместо Plotly.
- При одном проекте main показывает ещё сводную гистограмму/таблицу по проекту — в showcase сразу месяцы (принято).

---

## §5 БДДС расходы (план, факт, уточненный план)

### Статус приёмки 02.08.2026 (цикл регресса)

✅ Принят на стенде 02.08.2026 (цифры + chrome + mobile ~390 + редактор лотов).

Коммиты: `0c09fa2` (pipeline + editor), `7a361e7` (CI types), `5b49be6` (sticky A/B/C), `533d0c8` (mobile width).

### Сверка цифр (Проект = Все, 23.03.2024 – 30.07.2029, база план)

| | план | факт | прогноз | откл. |
|---|-----:|-----:|--------:|------:|
| Main / showcase стенд | **6716.1** | **3889.8** | **6716.1** | **0.0** |

API `parity=main_dashboard_forecast_budget`. Одиночный проект без сумм MSP (напр. Дмитровский) → нули в сводной/графике при «скрыть нули»; факт 1С виден в «Статус» (как main).

### Chrome

Фильтры group/view/period/dev_base/hide_deviation/hide_zero; график план/факт/прогноз + подписи млн ₽; таблицы периода и Статус; Скачать/зум; при одном проекте — редактор лотов (сессия) + «Пересчёт по лотам».

---

## §6 Контрольные точки

### Статус приёмки 03.08.2026 (цикл регресса)

✅ Принят на стенде 03.08.2026 (цифры + desktop fit + mobile cards/pills).

Сверка: Дмитровский ГПЗУ **+91**, Экспертиза **−19**, финанс. **+304**; Есипово ГПЗУ **−270**; РС **+4**, СМР **−35**, ЗОС **−80** = main. Smoke ALL OK.

Коммиты: `b7d0eff` (mobile kit), `8ac5add` (границы), `1e9b8a8`/`a074d6a` (таблицы в ширину карточки). Пилот mobile-kit для раската на остальные экраны.

Открытый долг без блокера: клик по статус-кружку (MSP) в Next не перенесён.

### Черновик сверки 31.07.2026

Статус: ✅ принято на стенде 31.07.2026 (`99c65dc`).

- Источник API: активная версия `web_data.db` через `core_bridge` и функции
  `dev_projects_tz_matrix` из main; чтение `web/` удалено.
- Chrome: свёрнутый по умолчанию блок «Фильтры», выбор одного проекта, «Сбросить»,
  без KPI-карточек и графиков.
- Матрица разбита на три отдельные карточки: ГПЗУ / Экспертиза стадии П / Начало
  финансирования / Стадия РД; РС / Завершение СМР / Пуск электричества / Пуск газа;
  ЗОС / РВ / Право 1 / Выкуп ЗУ / Право 2.
- У каждой вехи четыре подколонки: статус, План, Факт, Откл.; сетка, цвет отклонений,
  оранжевые даты для 100% и скачать/зум перенесены по паттерну main.

### Проверить перед приёмкой

1. На стенде шесть строк проектов и порядок/этапы совпадают со скрином main.
2. Даты, статусы и знаки отклонений совпадают в каждой из трёх карточек.
3. Фильтр, сброс, CSV/xlsx и полноэкранный режим работают в светлой и тёмной темах.

### Открытые отличия

- Детализация MSP по клику на кружок, доступная в Streamlit, в Next пока не переносилась.

---

## §7 График проекта

### Регресс 03.08.2026

Статус: ✅ принято на стенде 03.08.2026 (`51b4758`).

- Ковенанты: ромбы plan/fact как main; WBS ancestor-блок через `import_renderers_module`; cache `v4`.
- Desktop: отступ дат от ромбов (`COVENANT_LABEL_GAP_MS`).
- Mobile: compact gantt (без дат на оси); таблица `MobileEntityCard` / `MobileMetricGrid`.

### Регресс 03.08.2026 — в работе

- Desktop `lg+`: Tremor заменён на Plotly для grouped bar по проектам/контрагентам, pie и динамики; график контрагентов выводит весь список в горизонтальном скролле.
- Отклонение передаётся API со знаком: высота бара строится по модулю, подпись и цвет — по знаку (красный `< 0`, зелёный `> 0`).
- KPI-ряд showcase удалён. На `<lg` широкие таблицы заменены карточками; фильтры-чипы сохранены.
- Не принято: требуется локальная сверка desktop/mobile и приёмка на стенде.

### Черновик 31.07.2026

Статус: ✅ принято на стенде 31.07.2026 (`81e280f`).

- Источник: активная версия `web_data.db` через `core_bridge.load_msp_frame` + `project_labels`; чтение `web/` из `services/project_schedule.py` убрано.
- Chrome: H1 «График проекта», свёрнутый блок «Фильтры» + «Сбросить»; селекты Проект / Функциональный блок / Строение / Уровень; чекбоксы причин, лотов, %, скрыть 100%, только откл. окончания.
- Жёлтый баннер при нескольких проектах / cap ганта (600).
- Гант: колонка имён слева (~1:3), линии справа без наложения; скрыты выбросы дат и нулевые полосы (начало=конец); hover с датами; зум.
- Таблица задач: сетка, подсветка откл. как main, сортировка колонок, CSV/xlsx.

### Проверить в UI

1. Фильтры свёрнуты по умолчанию; сброс возвращает «Все» / «Верхний уровень» / чекбоксы off.
2. Один проект → нет жёлтого баннера (если строк ≤ 600); «Все» → баннер и гант ≤ 600, таблица полная.
3. Уровень / блок / строение / 100% / откл.>0 меняют набор строк; «Ковенанты» игнорит уровень.
4. Гант: две полосы teal/orange, даты, зум; таблица колонки как main; скачать CSV+xlsx.
5. Светлая/тёмная тема без утечек chrome.

### Открытые отличия / техдолг

- Проект — одиночный select («Все»/один), не multiselect main.
- Подпись чекбокса откл. окончания `< 0` и фильтр `base end − plan end < 0` — как в main.
- Ancestor-фильтр блока / WBS-разворот «причин» упрощены относительно полного `_renderers`.
- Plotly-гант на клиенте (не `gantt_grouped_figure` main); режим ковенантов-ромбов не отделён.

---

## §8 Причины отклонений

### Регресс 03.08.2026

Статус: ✅ принято на стенде 03.08.2026 (`182242d`).

- Desktop 1:1 Plotly bar+pie+facets+stack; черта y=0; modebar как main; download portal.
- Mobile: cards на таблицах; modebar скрыт, только fullscreen.
- Светлая тема по умолчанию (`theme_v3`).

### Черновик 31.07.2026

Статус: ✅ принято на стенде 31.07.2026 (`3782ca0`).

- Источник: активная версия `web_data.db` через `core_bridge.load_msp_frame` + `project_labels`; чтение `web/` из `services/deviation_reasons.py` убрано.
- Chrome: H1 «Причины отклонений», свёрнутый блок «Фильтры» + «Сбросить»; селекты Проект / Функциональный блок / Строение / Причина; период (date range); чекбокс «ТОП 5 причин отклонений».
- Вкладки: «Доли причин отклонений по проекту» | «Динамика причин отклонений по месяцам».
- KPI: Основная причина / Доля основной причины (`N% (count)`).
- Вкладка «Доли»: столбчатый Tremor BarChart «Причины отклонений (за отчетный период)» (подпись count+% в категории) + DonutChart с легендой; зум.
- Таблица «Детальные данные»: колонки как main (ID / Проект / блок / Название / Строение / Окончание / Базовое окончание / Отклонение / Причина / Заметки); подсветка дат/откл. голубым; отрицательные откл. красным; сортировка ⇅; CSV+xlsx.
- Вкладка «Динамика» (как main `dashboard_dynamics_of_deviations` + hide_shared_filters): фасеты Tremor BarChart на проект (стек по бакетам причин) → таблица «Число отклонений по проекту и месяцу» + ИТОГО → общий стек период×проект → сводная проект/причина/кол-во/дни + ИТОГО; CSV+xlsx у таблиц; будущие месяцы plan end отсекаются.

### Проверить в UI

1. Фильтры свёрнуты по умолчанию; сброс возвращает «Все» / период на min–max / TOP-5 off.
2. KPI и доли совпадают со Streamlit при тех же фильтрах; TOP-5 сужает только графики.
3. Вкладка динамики: фасеты-графики по проектам (стек причин) → таблица «Число отклонений по проекту и месяцу» с ИТОГО → общий стек период×проект → сводная проект/причина с днями и ИТОГО; скачать CSV+xlsx у обеих таблиц.
4. Таблица «Доли»: голубой фон у Окончание / Базовое / Отклонение; красный шрифт при откл. < 0; скачать CSV+xlsx; зум.
5. Светлая/тёмная тема без утечек chrome.

### Открытые отличия / техдолг

- Подписи count+% на столбцах — через перенос в подпись категории Tremor (не Plotly `textposition=outside`).
- Donut Tremor вместо полного pie main (`hole=0`); цвета сегментов — палитра Tremor + легенда с bucket-цветами.
- Ancestor-фильтр блока / полный WBS «Строение» упрощены (как у графика проекта).
- Колонки таблицы — как в `_render_deviations_maket_table` main («Окончание»/«Базовое»/«Заметки»), не OCR-варианты «План./Фактич./Куратор».
- История снимков / ось «по дате снимка» на динамике не вынесена (в combined UI main зафиксирована ось по plan end).
- Динамика: фасеты по проектам (стек причин) + таблица «Число отклонений по проекту и месяцу» + стек период×проект + сводная проект/причина с ИТОГО и CSV+xlsx; без Plotly-итогов над столбцами и без подписей inside сегментов.

---

## §9 Отклонение от базового плана

### Регресс 03.08.2026

Статус: ✅ принято на стенде 03.08.2026 (`50b6ad6`…`de0fa01`).

Сверка скринов 03.08 (1–2 main, 3–4 showcase):

| Блок | main | showcase | |
|------|------|----------|--|
| ЗОС (−65/−24/−182/−100/−1 · макс 729/792/1004/694/275) | ✓ | ✓ | 1:1 |
| Режим «причины» (bars + таблица причин) | ✓ | ✓ | OK |
| Режим «ковенанты» (точки + «Ковенанты (таблица)» + полная) | ✓ | ✓ | OK |
| Фильтры 5×5 уровни | ✓ | kit `03c9bac` | OK |
| Mobile таблицы → блоки + подсветка | — | ЗОС/ков/полная cards | OK |
| ⛶ vs Plotly modebar | — | кнопка под modebar | OK `de0fa01` |

- Режим «Только ковенанты»: `chart.kind=covenant_points` (4 серии: базовое начало/окончание, начало/окончание — circle/diamond); caption «Блок «Ковенанты»: …точками»; таблица «Ковенанты (таблица)» + expander «Полная таблица отклонений…».
- Фильтры: общий kit `dashboard-filters` (5 select + 5 checkbox на одних уровнях).
- Mobile: ЗОС / ковенанты / полная таблица — карточки с highlight дат/откл.

### Черновик 31.07.2026

Статус: ✅ принято на стенде 31.07.2026 (`297fb8d`).

- Источник: активная версия `web_data.db` через `core_bridge.load_msp_frame` + `project_labels`; чтение `web/` убрано (`KNOWN_DEBT` #9 снят).
- Chrome: H1 «Отклонение от базового плана», свёрнутый блок «Фильтры» + «Сбросить».
- Фильтры (как main `dashboard_plan_fact_dates`): Проект / Функциональный блок / Строение / Детализация (ур.4|5) / Причина (категория); чекбоксы «Показать причины», «Скрыть 100%», «Только ковенанты», «только откл. окончания < 0», «Откл. длительности»; radio подписей name|lot при наличии колонки лота.
- Блок «ЗОС»: карточки по проектам — План окончания (base end) / Факт (plan end) / Отклонение / Максимальное |откл.| дней; красный при просрочке.
- График: Plotly horizontal grouped bars teal/orange от начала шкалы до дат окончания; даты на концах полос; легенда снизу; подпись-caption как main (РД ур.5+шифр); наибольшее отклонение сверху; зум.
- Таблица «Причины отклонений (таблица)» (дефолт show_reasons): ID / Проект / блок / Название / Строение / Окончание / Базовое / Отклонение / Причина / Заметки; голубой фон дат/откл.; красный шрифт откл.<0; сортировка ⇅; CSV+xlsx.

### Проверить в UI

1. Фильтры свёрнуты; «Сбросить» → Все / ур.4 / причины on / остальные off.
2. ЗОС: по карточке на проект при «Все»; даты и отклонение совпадают с main при том же срезе.
3. График: teal=Базовое окончание, orange=Окончание; даты на концах; caption про РД; зум.
4. Таблица причин: голубая подсветка Окончание/Базовое/Отклонение; красный при <0; скачать CSV+xlsx.
5. Выкл. «Показать причины» → таблица дат/длительностей; детализация ур.4|5 активна.
6. Светлая/тёмная без утечек chrome.

### Открытые отличия / техдолг

- РД-агрегация графика портирована в сервис (не полный импорт `_renderers` — shim); при пустом РД-срезе fallback на строки фильтров.
- Ancestor «Строение» / L3 — упрощённый стек уровней, не полный WBS main.
- Подпись колонки причины — «Причина отклонения» (как maket main), не OCR «Причины и пояснения».

---

## §10 Проектная документация

### Регресс 03.08.2026

Статус: ✅ принято на стенде 03.08.2026 (регресс).

- Desktop (`lg+`): pie «Исполнение ПД»; line «Динамика» `lines+markers+text` + цвета main; monthly overlay жёлтый/зелёный «+N»; **Gantt Plotly** жёлт/зелён/красн + даты у полос (`942c375`…`0dbd50f`).
- Mobile: 3 таблицы → `MobileCardStack` + highlight; графики можно облегчить.
- Правило цикла (все дашборды): desktop Plotly = main; mobile — облегчение где уместно (`PARITY_FULL_REGRESSION_PLAN.md`).
- Техдолг: single-select период/проект vs multi main.

### Черновик 31.07.2026

Статус: ✅ принято на стенде 31.07.2026.

- Источник: активная версия `web_data.db` через `core_bridge.load_msp_frame` + `project_labels`; чтение `web/` убрано (`KNOWN_DEBT` documentation.py снят).
- Маршрут: `/docs/project-documentation`; API `GET /api/project-documentation`.
- Chrome: H1 «Проектная документация»; tabs «Проектная документация» | «Просрочка выдачи ПД»; фильтры свёрнуты + «Сбросить».
- Вкладка 1: Проект / Период / Вид раздела; легенда статусов (зелёный/красный); Donut «Исполнение ПД»; KPI ряд (план БП / план на дату / факт / отклонение / производительность); Line «Динамика выдачи ПД» (План БП / Прогноз / Факт); таблица выдачи по проектам + CSV/xlsx + сортировка; зелёная подсветка опережения.
- Вкладка 2: Проект / Отображение / Вид раздела / статус-тег «Просрочка по утверждению» / дата; Gantt Base/Finish/Просрочка; карточки проектов; cumulative bar по месяцам; детальная таблица + сводка по просрочке (красный/зелёный фон); скачать.

### Проверить в UI

1. Фильтры свёрнуты; «Сбросить» → Все / Все месяцы / Все разделы.
2. Donut: Завершено / Не начато (и «В работе» если есть); подпись «График Исполнение ПД».
3. KPI: отклонение зелёный при ≥0 / красный при <0; производительность за неделю.
4. Line: три серии План БП / Прогноз / Факт; зум.
5. Таблица: № / Проект / Раздел ПД / Базовое / Окончание / Отклонение; зелёная подсветка ahead; скачать CSV+xlsx; сортировка ⇅.
6. Вкладка просрочки: Gantt жёлтый/зелёный/красный; карточки «Просрочка N док.» / «Без просрочки».
7. Динамика по месяцам (горизонт. bar); детальная + сводка; скачать обе.
8. Светлая/тёмная без утечек chrome.

### Открытые отличия / техдолг

- Gantt — CSS-полосы (не полный Plotly duration figure main); подписи дат на полосах упрощены.
- Donut Tremor вместо Plotly pie main; гранулярность вынесена в фильтры (в main — selectbox у графика).
- Период — single select месяцев снимка (main — multiselect).
- Статус-тег вкладки 2 — фиксированная плашка «Просрочка по утверждению» (как на скрине), не полный multi-pills main.
- РД (#11) — отдельный сервис `working_documentation.py` (rd_plan+tessa из БД).

---

## §11 Рабочая документация

### Регресс 03.08.2026

Статус: ✅ принято на стенде 03.08.2026 (`3e03a99`…`c9e115f`).

- Lookup дат: CSV `other_*_rd.csv` + DB fallback → overdue **~520**; avg ~338–345.
- Desktop: Plotly pie/line/monthly/gantt; цвета отклонений в таблице; Gantt без наложения дат.
- Фильтры: shared BDDS-чипы (+ scroll при >7) на РД и остальных дашбордах.
- Mobile: `MobileCardStack` detail main+delay.

### Черновик 31.07.2026

Статус: ✅ принято на стенде 31.07.2026.

- Источник: активная версия `web_data.db` через `core_bridge.load_version_df` — `rd_plan`, `tessa`, `tessa_tasks`, `debit_credit`. Файловый `_r23_12_load_rd_plan_lookup` (чтение `web/`) отключён; даты договора/прогноза — `_rd_plan_db_contract_lookup`.
- Маршрут: `/docs/working-documentation`; API `GET /api/working-documentation`.
- Chrome: H1 «Рабочая документация»; tabs «Рабочая документация» | «Просрочка выдачи РД»; фильтры свёрнуты + «Сбросить».
- Вкладка 1 «План выдачи РД»: Проекты / Раздел / Период / Статус (chips) / radio кол-во|% / checkbox прогнозной даты; KPI Всего / С отклонением / Ср. задержка; Pie «Исполнение РД» (4 статуса TESSA); «Динамика по месяцам» Plan/Fact cumulative; блок «Динамика выдачи РД» (KPI план/факт/отклонение/производительность + Line); детальная таблица + CSV/xlsx.
- Вкладка 2 «Просрочка выдачи РД»: те же KPI; Gantt (жёлтый договор / зелёный прогноз / красная просрочка); динамика по месяцам; детальная таблица + скачать.

### Проверить в UI

1. Фильтры свёрнуты; «Сбросить» → Все проекты/разделы/статусы, весь период, кол-во разделов, прогноз on.
2. KPI: Всего разделов / С отклонением / Ср. задержка (локально ~539 / ~504 / ~332.8 при DB-only lookup; скрин main 539/520/337.3 с файловым lookup).
3. Pie: Выдано в производство (~321) / Не выдано (~119) / На рассмотрении у ГИП (~56) / Возвращено на доработку (~43); цвета зелёный/розовый/жёлтый/красный.
4. Динамика по месяцам — горизонт. bars Plan/Fact; Line План(синий)/Факт(оранжевый) + KPI отклонение.
5. Детальная таблица: сортировка ⇅; статус — цвет фона; отклонения — градиент (<0 красный, ≥0 зелёный); «Скачать таблицу».
6. Вкладка просрочки: Gantt по проектам (Ленинский / Есипово-5 / Дмитровский-1); фильтр «Отображение» По проекту|По разделу.
7. Светлая/тёмная без утечек chrome.

### Открытые отличия / техдолг

- DB-only lookup дат даёт KPI «С отклонением» чуть ниже скрина main (нет `web/other_*_rd.csv`).
- Gantt — CSS-полосы (не Plotly duration figure main).
- Pie/Line — Tremor вместо Plotly.
- Фильтр статусов — chips; период — select + date inputs (не полный Streamlit date_input).

---

## §12 ГДРС (люди)

### Регресс 03.08.2026

Статус: ✅ принято на стенде 03.08.2026 (`e79d9a8`…`c80e82a`, CloudPub).

- Desktop: Plotly grouped bars (проекты + контрагенты h-scroll), pie, line динамики (`gdrs-charts.tsx`); deviation = |Δ| вверх, знак в подписи/цвете; шрифты light как main.
- KPI-карточки сверху убраны (как main renderer).
- Таблицы проектов/контрагентов: строка «Итого»; tint plan/skud/dev.
- Фильтры: shared chips + scroll (>7); mobile bottom sheet.
- Mobile: Plotly `compact` (pie HTML-легенда; контрагенты h-scroll; динамика без point-labels + scroll при «День») + `MobileCardStack`.
- Техдолг: audit CSV expander main; полный contractor filter options.

### Черновик 31.07.2026

Статус: ✅ принято на стенде 31.07.2026 (`0c4bf2a` / ship `6cfefcb`).

- Источник: активная версия `web_data.db` через `core_bridge` — `gdrs_fact`, `dogovor_json`, `spravochniki_json`, `kontr_json`, `reference_dannye`. Чтение `web/` убрано (`KNOWN_DEBT` gdrs.py снят).
- Маршрут: `/gdrs/people`; API `GET /api/gdrs-people` (timeout UI 300s).
- Chrome: H1 «ГДРС (люди)»; фильтры свёрнуты + «Сбросить»; Проект / Контрагент / Месяц / План / СКУД / «Только с планом».
- Блоки 1:1 с main: bar+таблица проектов → donut контрагентов → сводная матрица (недельные колонки при «Среднее за месяц» + 1 месяц) → bar контрагентов → Line динамики + таблица периодов → таблица распределения.
- Таблицы: `gdrsPalette(dark)` — светлые pastel / тёмные rgba; текст `thFg`/`cellFg`; donut без slate/gray; KPI `deviationStyle(..., dark)`.
- OOM: `lru_cache` на fact/plan/dannye; без fuzzy canonicalize на каждый запрос; без полного `gdrs_contractor_filter_options`.

### Проверить в UI

1. Фильтры свёрнуты; «Сбросить» → все проекты/контрагенты, дефолтные месяцы, Среднее за месяц, только с планом off.
2. KPI план/СКУД/отклонение/%; отклонение зелёный ≥0 / розовый <0.
3. Bar «ГДРС по выбранным проектам» (синий/зелёный/красный) + таблица под ним с tint и ⇅ + скачать.
4. Donut «Распределение людей по контрагентам» (топ-10 + Прочие) + зум.
5. Матрица «ГДРС (люди)»: зелёный план / синий СКУД / красный откл.; недельные колонки при одном месяце; Итого/субтоталы; скачать.
6. Bar по контрагентам; Line «Динамика людей» (День/Неделя/Месяц) + «Детализация по периодам» с tint.
7. Таблица распределения с долей % и tint.
8. Светлая/тёмная без утечек chrome.

### Открытые отличия / техдолг

- Список контрагентов — из fact∪plan, не полный `gdrs_contractor_filter_options`.
- Аудит-выгрузки CSV (expander main) не перенесены.

---

## §13 ГДРС (техника)

### Регресс 03.08.2026

Статус: ✅ принято на стенде 03.08.2026 (идентичен §12 UI; smoke CloudPub `/gdrs/equipment`).

- Общий `GdrsView` + `gdrs-charts` с `resourceKind=equipment`; фиксы §12 (Plotly compact, pie legend, dynamics) наследуются.
- Данные: `vid=Техника` / API `GET /api/gdrs-equipment`.

### Черновик 31.07.2026

Статус: ✅ принято на стенде 31.07.2026 (`0c4bf2a` / ship `6cfefcb`).

- Тот же сервис `gdrs.py` с `resource_kind=equipment` / `vid=Техника`; маршрут `/gdrs/equipment`; API `GET /api/gdrs-equipment`.
- UI-компонент общий (`GdrsView` + `resourceKind`); подписи «техника» / «Динамика техники» / «Распределение техники…».
- Паритет блоков и chrome — как §12.

### Проверить в UI

1–8 как в §12, с заменой «людей» → «техники» (на скрине main — «механик»/техника).
9. KPI и строки матрицы отличаются от людей (другой `vid_resursa`).

### Открытые отличия / техдолг

- Те же, что §12.

---

## §14 Предписания по подрядчикам

### Регресс 04.08.2026 (полный проход)

Статус: ✅ принято на стенде 04.08.2026 (`597eeaf`).

- Фильтры desktop: **native select как main** (`FilterChipSelect`/`FilterChipMulti` dual-render
  на `lg+` + автокомплит № договора + период двумя датами). Mobile — chips + bottom sheet.
  Набор: Проект, Подрядчик, № договора, Период, чекбокс «Не отображать устраненные».
- «Исполнитель (согласование)» в текущем main — dead code (`sel_curator`), не добавляли.
- KPI: полоска 4× + «Ключевые показатели» справа; v6: 144/33/111/16, overdue 106, crit/stop 0.
- Plotly 1:1: h-bar подрядчиков (оранжевый сегмент + синий пузырёк), pie статусов 5 бакетов,
  stacked bar объектов; `prescriptions-charts.tsx`.
- Mobile: графики `compact`; детальная таблица → карточки (`MobileEntityCard`), без h-scroll.

### Локально готово 31.07.2026 (предыдущий цикл)

Статус: ✅ принято на стенде 31.07.2026 (`5ff7bd8`).

- Источник API: активная версия `web_data.db` через `core_bridge` — `tessa`,
  `tessa_tasks`, `projekts_json`, `dogovor_json`; чтение `web/` удалено.
- Пайплайн повторяет `dashboard_predpisania` (helpers `_pred_*`): KindName «Предписания»,
  реестр Projekts, отменённые и «Проект» исключаются, дедуп DocID, устранено =
  KrStateID 13, факт из «Проверка»/«Принято», `_overdue_days` со знаком как в main.
- Chrome: свёрнутые фильтры, multiselect, № договора, период, «Не отображать
  устраненные…», «Сбросить», FullscreenPanel, CSV/xlsx.
- Графики: горизонтальный bar подрядчиков, donut 5 бакетов статусов, stacked bar объектов.

### Проверить на стенде

1. KPI при полном срезе и при фильтрах совпадают с main; локальная active version 4:
   всего 119, устраненные 32, неустраненные 87, непросроченные 13, просроченные
   неустраненные 84, критические 0, остановка работ 0 (скрин main 139/33/106 — другой срез данных).
2. Фильтры свёрнуты по умолчанию; multiselect / № договора / даты / скрытие устранённых /
   «Сбросить» корректно меняют все блоки.
3. Горизонтальный график подрядчиков, donut с палитрой и stacked chart объектов —
   светлая/тёмная тема + fullscreen.
4. Детальная таблица: колонки main, status chip, подсветка строк, сортировка, CSV/xlsx.

---

## §15 Исполнительная документация

### Локально готово 31.07.2026

Статус: ✅ принято на стенде 31.07.2026.

- API `executive_docs_db.py`: active `web_data.db` через `core_bridge` + helpers `_exec_*` / `_tessa_*` из `_renderers`.
- Фильтры: проект, контрагент, вид документа, период, гранулярность (Месяц), чекбокс скрытия просрочки (default on), Сбросить.
- Chrome: справочник видов ИД, 6 KPI, просрочки подрядчик/заказчик (rose/amber), вкладки Накопительным итогом / Детальный отчёт / Динамика.
- Локально version 4: всего 132, отказы 0, на согласовании 13, принято 1, у подрядчика 48, просрочек 61 (скрин main ~134/0/10/1/48/58 — другой срез).

### Проверить в UI / на стенде

1. KPI и buckets просрочек близки к main на том же срезе данных.
2. Три вкладки: статусы+объекты / детальная таблица со скроллом и chip / динамика по месяцам.
3. Фильтры + справочник + скачать + fullscreen; светлая/тёмная тема.
4. Цвета Tremor named (`rose`/`amber`/`teal`/`blue`) — не чёрные сегменты.

---

## §17 Настройки профиля

### Локально готово 31.07.2026

Статус: ✅ принято на стенде 31.07.2026.

- UI `/settings/profile`: метрики Пользователь/Роль, вкладки «Изменить пароль» | «Изменить email» (emerald underline).
- API: `POST /api/auth/login`, `GET /api/auth/me`, `POST /api/profile/password`, `POST /api/profile/email` через `users_bridge` → `auth`/`logger`.
- `users.db`: `BI_USERS_DB` (docker: `/data/db/users.db`), seed `admin` / `BI_DEMO_ADMIN_PASSWORD` (default `admin`), email `admin@example.com`.
- Сессия: legacy `demo`/Аналитик мигрирует в `admin` / Суперадминистратор; вход `admin` / `admin`.

### Проверить в UI

1. Вход `admin` / `admin` → профиль показывает Суперадминистратор и email.
2. Смена пароля (3 поля show/hide) и email; ошибки валидации.
3. Footer «К отчётам» / «Выйти»; активная ссылка в сайдбаре «Настройки профиля».

---

## §18 Административная панель

### Локально готово 31.07.2026

Статус: ✅ принято на стенде 31.07.2026.

- UI `/settings/admin`: primary «Фильтры отчетов» | «Управление системой»; secondary как в main.
- **Фильтры:** Настроить / Просмотр всех / Копировать — CRUD через `filters.py`.
- **Система:** Пользователи (add/role/delete), Статистика (4 KPI + роли), Логи (фильтры + DownloadTableButton), Права (ROLES), Конфигурация (email admin, baseline task, JSON вех/матрицы).
- **Данные:** блок FTP/ingest/token в админке; сайдбар «FTP + перезагрузить БД» + список версий (зелёная активная / коричневые предыдущие с датой-временем выгрузки). Sync/activate — admin-токен **или** `X-Auth-User` (роли ftp как в main).
- API: `/api/settings/*` + `X-Auth-User`; мутации — роль admin/superadmin.

### Проверить в UI

1. Доступ только admin/superadmin; analyst → сообщение об отказе.
2. Все sub-tabs системы и фильтров; создание/смена роли пользователя; логи с фильтрами и скачиванием.
3. Конфигурация: сохранение admin email и baseline task.
4. FTP-блок внизу: статус, sync/ingest с токеном.
5. «← Вернуться к отчетам» → `/developer-projects`.

---
